import React from 'react';
import {Card} from "@/components/ui/card";

const SelectedTeamCard = () => {
    return (
        <Card className="mt-4 flex justify-center items-center gap-2 font-pixellari bg-dark text-violet font-medium rounded-2xl p-4 border-none h-[184px] uppercase">
            SELECT
        </Card>
    );
};

export default SelectedTeamCard;